name: lint

on: [push, pull_request]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: bash syntax check
        run: bash -n scripts/amap.sh
      - name: shellcheck
        run: shellcheck scripts/amap.sh
